# Audio2txt v4.0 Enterprise Configuration

app:
  name: "audio2txt-enterprise"
  version: "4.0.0"
  debug: true

cloud:
  provider: "assemblyai"
  region: "global"

export:
  formats: ["txt", "pdf", "md"]
